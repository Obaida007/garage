# تثبيت كراج البارودي (Al-Baroudi Garage)

هذا الدليل للمستخدم النهائي. لا تحتاج معرفة برمجية.

## 1. تثبيت البرنامج

1. انسخ ملف التثبيت `Al-Baroudi-Garage-Setup.exe` إلى جهاز الكاشير.
2. انقر نقراً مزدوجاً على الملف.
3. إذا ظهرت رسالة SmartScreen اختر المزيد من المعلومات ثم تشغيل على أي حال (إن كنت تثق بمصدر الملف).
4. أكمل معالج التثبيت.
5. بعد الانتهاء ستجد **كراج البارودي** على سطح المكتب وفي قائمة ابدأ.

## 2. توصيل الشاشة الثانية

1. وصّل شاشة الزبائن بكابل HDMI أو DisplayPort أو USB-C.
2. من إعدادات Windows ← النظام ← العرض:
   - تأكد أن الشاشة الثانية ظاهرة.
   - اختر **توسيع هذه العروض** وليس تكرار.
3. اجعل شاشة الكاشير هي الشاشة الرئيسية.
4. شغّل البرنامج. شاشة الانتظار تفتح وحدها على الشاشة الثانية بملء الشاشة.

إذا لم توجد شاشة ثانية يعمل البرنامج بشكل طبيعي على شاشة الكاشير فقط.

## 3. اختيار الطابعة

1. وصّل الطابعة الحرارية (58mm أو 80mm) وثبّت تعريف Windows الخاص بها.
2. افتح كراج البارودي ← الإعدادات.
3. اختر الطابعة وحجم الورق.
4. احفظ الإعدادات.

## 4. تشغيل البرنامج

انقر مرتين على أيقونة **كراج البارودي**.

لا تفتح المتصفح ولا تشغّل أوامر. كل شيء داخل التطبيق.

## 5. اختبار الطباعة

1. من الإعدادات اضغط **اختبار الطباعة**.
2. أو من شاشة الكاشير اضغط **دور جديد**.
3. يجب أن تخرج تذكرة برقم الدور.
4. إذا ظهر «تم إنشاء الدور لكن تعذر الطباعة» يبقى الرقم صالحاً. استخدم **إعادة طباعة**.

## 6. اختبار شاشة الانتظار

1. من الإعدادات اضغط **اختبار شاشة الانتظار**.
2. أنشئ دوراً ثم اضغط **استدعاء التالي**.
3. يجب أن يتحدث رقم الدور والحفرة على شاشة الزبائن فوراً.

## تشغيل تلقائي مع ويندوز

من الإعدادات فعّل خيار التشغيل مع Windows حتى يبدأ النظام بعد إعادة تشغيل الجهاز، بما في ذلك شاشة الانتظار إن كانت متصلة.

## النسخ الاحتياطي

من الإعدادات يمكن حفظ نسخة `garage-backup-YYYY-MM-DD.db` واستعادتها عند الحاجة. الاستعادة تطلب تأكيداً لأنها تستبدل البيانات الحالية.
